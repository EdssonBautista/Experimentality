server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto:  update
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.MySQL5Dialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:  jdbc:mysql://localhost:8889/experimentality?verifyServerCertificate=false&useSSL=true
    username: root
    password: root